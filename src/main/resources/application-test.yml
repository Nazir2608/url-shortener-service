spring:
  jpa:
    show-sql: false
  flyway:
    clean-disabled: false

logging:
  level:
    com.nazir.urlshortener: DEBUG
    org.testcontainers: INFO
    org.flywaydb: WARN

# Disable GeoIP in tests (no .mmdb file needed for unit tests)
geoip:
  enabled: false
